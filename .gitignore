node_modules/
.env
dist/

.env.cloud

certs/

# Built dashboard copied in for LAN failover access (see src/server.js).
# A build output, regenerated by `npm run build` in the frontend repo.
public/

# Timestamped backups of the env files. Same credentials, same risk.
.env*.bak.*
.env.cloud.bak.*
*.bak
.env*
!.env*.example
