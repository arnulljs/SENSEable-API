{
  "name": "senseable-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Barebones backend for SENSEable — ingests raw ADC telemetry (frozen wire protocol), applies calibration, serves the React dashboard's data shape over REST.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "simulate": "node scripts/simulate.js",
    "check": "node --check src/server.js && node --check src/store.js && node --check src/calibration.js && node --check src/ingest.js && node --check src/status.js && node --check src/routes.js && node --check src/mqtt.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "mathjs": "^15.2.0",
    "mqtt": "^5.10.3",
    "pg": "^8.22.0"
  }
}
